<UserControl x:Class="ExcelAddIn.Panes.InputPaneWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExcelAddIn.Panes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid x:Name="imageArea" Visibility="Collapsed">
            <Border Background="White"></Border>
            <Image x:Name="imageAreaimgItem" Stretch="None" OpacityMask="White" VerticalAlignment="Top"/>
        </Grid>
        <Grid x:Name="listArea" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Border Background="White"/>
            <Image Grid.Row="0" x:Name="listAreaimgItem" Stretch="None" OpacityMask="White"/>
            <Image Grid.Row="1" x:Name="listAreaListHedaerItem" Stretch="None" HorizontalAlignment="Left"/>
            <ScrollViewer Grid.Row="2" HorizontalAlignment="Left">
                <Image x:Name="listAreaListItem" Stretch="None"/>
            </ScrollViewer>
        </Grid>
        
    </Grid>
</UserControl>
